# ── RSP Web · configuración ─────────────────────────────────────────────
# Copia este archivo a .env.local y ajusta los valores.

# URL del backend RSP que expone las APIs JSON (admin-placeta).
VITE_API_URL=http://localhost:3002

# true  → proveedor local con DATOS REALES (snapshot del banco + lectura en vivo). Por defecto.
# false → habla directamente con el backend real en VITE_API_URL (BFF).
VITE_USE_MOCK=true

# Prefijo de nombre de aplicación (se usa en títulos y metadatos).
VITE_APP_NAME=RSP
