<div class="junior-section">
  <div style="background:linear-gradient(135deg,#880e4f,#e91e63);color:#fff;border-radius:16px;padding:24px 28px;margin-bottom:16px">
    <div style="display:flex;align-items:center;gap:16px;flex-wrap:wrap">
      <div style="width:64px;height:64px;background:rgba(255,255,255,.15);border-radius:14px;display:flex;align-items:center;justify-content:center;font-size:32px">👤</div>
      <div>
        <h4 style="font-size:20px;font-weight:700">Gestión de Tutores</h4>
        <p style="font-size:13px;opacity:.7"><%= tutores.length %> tutor<%= tutores.length !== 1 ? 'es' : '' %> registrado<%= tutores.length !== 1 ? 's' : '' %></p>
      </div>
    </div>
  </div>

  <% if (tutores.length === 0) { %>
  <div class="info-card mb-16" style="text-align:center;padding:40px">
    <div style="font-size:48px;margin-bottom:12px">👤</div>
    <h3 style="font-size:16px;margin-bottom:8px;color:#333">Sin tutores registrados</h3>
    <p style="font-size:13px;color:#888;margin-bottom:16px">Los tutores legales aparecerán aquí cuando se vinculen a menores.</p>
    <a href="/junior" class="btn btn-primary">📊 Volver al Dashboard</a>
  </div>
  <% } else { %>
  <div style="display:grid;gap:12px">
    <% tutores.forEach(t => { %>
    <div class="entidad-card" style="border-left:4px solid #e91e63">
      <div style="display:flex;align-items:center;gap:14px">
        <div style="width:44px;height:44px;background:#fce4ec;border-radius:12px;display:flex;align-items:center;justify-content:center;font-size:20px">👤</div>
        <div>
          <h4 style="font-size:14px;margin:0"><%= t.nombre || 'Tutor' %> <%= t.apellidos || '' %></h4>
          <p style="font-size:11px;color:#888;margin:2px 0 0">DIP: <%= t.dip || '—' %> · Email: <%= t.email || '—' %></p>
        </div>
      </div>
    </div>
    <% }); %>
  </div>
  <% } %>
</div>
