{
  "name": "rsp-web-api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "API/BFF de rsp-web: sirve el SPA y agrega datos reales (BOP + Banco) para sustituir a admin-placeta",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "node --test",
    "dry-run": "node scripts/dry-run-emision.mjs"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.4.1",
    "express": "^5.2.1"
  }
}
