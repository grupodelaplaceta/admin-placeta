<%# PARTIAL empty. Uso: include('parciales/empty', { icono, titulo, texto }) %>
<div class="empty-state">
  <div class="empty-icon"><%= icono || '📭' %></div>
  <h3><%= titulo || 'Sin datos' %></h3>
  <p><%= texto || '' %></p>
</div>
